'use client'

import SignIn from "../SignIn"

interface AuthProps{
     
}

const Auth:React.FC<AuthProps> = ({}) => {
    return(
        <div>
            <SignIn/>
        </div>
    )
}

export default Auth